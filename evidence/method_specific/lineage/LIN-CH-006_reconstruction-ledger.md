# LIN-CH-006 — Deterministic Same-Project Lineage Reconstruction Ledger

Status: **FROZEN BEFORE FORMAL COMPARISON AGAINST P2**  
Date: **2026-09-25**  
Challenge ID: `LIN-CH-006`  
Method: **Lineage / DSD 계보론**

## 1. Derivation basis

This ledger was reconstructed from:

```text
P0 Lineage Protocol v0.1
commit:
  f69f364985d604d2c883b14b2efa18535a6bbf6e
blob:
  0ef686f3987b590e67e07b9ee5e4861c31e6e1ef

P1 LIN-CH-005 strongest-reasonable baseline precommit
commit:
  d0c5b6c6d060c30a85856f93cbd53d4dc341515a
blob:
  91dc9f6aeab1a1b101354b0ebbb2c4ae0eb123e1

LIN-CH-006 precommit
commit:
  eaae98831363f9c4cdf4ad90fb6219e7362dbd85
blob:
  ae4a04ecf9273141cccbb7055cbcad416ae9d4cd
```

The LIN-CH-005 result artifact P2 was not used to derive the following ledger.

## 2. R1 reconstructed ledger — versioned identity-rule semantics

Frozen rule registry:

```text
ID-RULE-v1:
  applicable only t0 -> t1
  stable registry member preserves successor identity

ID-RULE-v2:
  applicable t1 -> t2
  registry transition disables stable-registry identity inheritance
  explicit successor relation required
```

Supplied transition relation:

```text
cA -> cA2
```

Reconstructed Lineage output:

```text
t0 -> t1:
  LINEAGE_SUCCESSOR_ESTABLISHED
  basis = ID-RULE-v1

t1 -> t2:
  LINEAGE_SUCCESSOR_ESTABLISHED
  basis = explicit supplied successor relation

V2_RETROACTIVE_USE:
  no

V1_EXTENSION_ACROSS_TRANSITION:
  no

RULE_VERSION_PROVENANCE:
  retained

SAME_LABEL_AS_IDENTITY_SUBSTITUTE:
  no

LITERAL_EQUALITY_CLAIM:
  no
```

Maximum R1 claim:

```text
The requested successors are established only under
the rule/relation source applicable to each frozen interval.
```

## 3. R2 reconstructed ledger — branch/merge and optional constraints

Frozen base relation:

```text
a -> b
a -> c

d -> f
e -> f
```

Reconstructed:

```text
BRANCH_RELATION:
  established

MERGE_RELATION:
  established

BASE_LINEAGE_RELATION:
  established

UNIQUE_SUCCESSOR_REQUIREMENT:
  unsatisfied

BIJECTION_REQUIREMENT:
  unsatisfied

CARDINALITY_CONSERVATION_REQUIREMENT:
  unsatisfied where frozen set cardinalities differ

BASE_RELATION_ERASED_BY_OPTIONAL_FAILURE:
  no

BRANCH_OR_MERGE_PROTOCOL_FAILURE:
  no

HIDDEN_UNIQUENESS_RULE_ADDED:
  no
```

Maximum R2 claim:

```text
The base relation remains a valid relation-valued succession
even when stronger optional uniqueness/bijection/cardinality
profiles are not satisfied.
```

## 4. R3 reconstructed ledger — prerequisite dependency closure

Frozen dependency structure:

```text
sort a:
  successor relation supplied

sort b:
  requires decoder DB-v3

DB-v3:
  unavailable
```

Reconstructed:

```text
SORT_A_SUCCESSOR_STATUS:
  LINEAGE_SUCCESSOR_ESTABLISHED

SORT_B_REQUIRED_PREREQUISITE:
  DB-v3

SORT_B_PREREQUISITE_AVAILABILITY:
  unavailable

DEPENDENT_COMPONENT_SUCCESSOR_STATUS:
  LINEAGE_SUCCESSOR_BLOCKED

DEPENDENT_STATE_OBLIGATION:
  LINEAGE_TASK_BLOCKED

EXPLICIT_NEGATIVE_INFERRED:
  no

PREREQUISITE_PROVENANCE:
  retained
```

Maximum R3 claim:

```text
The dependent claim is blocked by an unavailable required
prerequisite; it is neither established nor explicitly negated.
```

## 5. R4 reconstructed ledger — relation-algebra coherence

Frozen relations:

```text
L_01:
  (a0,a1)

L_12:
  (a1,a2)

direct L_02:
  (a0,a2)
  (a0,b2)
```

Computed composition:

```text
L_12 o L_01
  =
{(a0,a2)}
```

Coherence test:

```text
{(a0,a2)}
  subset
{(a0,a2),(a0,b2)}

COMPOSITION_INCLUSION:
  PASS
```

Reconstructed:

```text
LINEAGE_FAMILY_COHERENCE_STATUS:
  LINEAGE_FAMILY_COHERENT

DIRECT_LONG_INTERVAL_RELATION:
  retained separately

EXTRA_DIRECT_PAIR:
  (a0,b2)
  retained

COMPOSED_RELATION_EQUALS_DIRECT_RELATION_INFERRED:
  no

DIRECT_VS_DERIVED_PROVENANCE:
  retained
```

Maximum R4 claim:

```text
The supplied lineage family satisfies the frozen composition
inclusion rule, while the direct long-interval relation retains
information not generated by composition.
```

## 6. R5 reconstructed ledger — conflict / underdetermination / sidecars

### Q1

Frozen state:

```text
one successor-support record
one explicit successor-negation record
same frozen semantics
no precedence resolver
```

Reconstructed:

```text
Q1_LINEAGE_SUCCESSOR_STATUS:
  LINEAGE_SUCCESSOR_CONFLICTING
```

### Q2

Frozen state:

```text
two admissible relation schemas
different successor outcomes
no resolver
```

Reconstructed:

```text
Q2_LINEAGE_SUCCESSOR_STATUS:
  LINEAGE_SUCCESSOR_UNDERDETERMINED
```

### Q3

Frozen state:

```text
explicit supplied successor relation

sidecars:
  Tracking continuity
  Transformation map
  Comparison similarity
  Classification class
  Aggregate readout
  Reconstruction candidate
  Audit pass on source-record conformance
```

Reconstructed:

```text
Q3_LINEAGE_SUCCESSOR_STATUS:
  LINEAGE_SUCCESSOR_ESTABLISHED

Q3_ESTABLISHMENT_BASIS:
  explicit supplied successor relation

TRACKING_SIDECAR_USED_AS_IDENTITY_CRITERION:
  no

OTHER_NEIGHBORING_SIDECARS_USED_AS_IDENTITY_CRITERIA:
  no
```

Frozen task precedence:

```text
OUT_OF_SCOPE
> CONFLICTING
> UNDERDETERMINED
> BLOCKED
> ESTABLISHED / PARTIAL / NOT_ESTABLISHED
```

Reconstructed run terminal:

```text
LINEAGE_TASK_TERMINAL_STATUS:
  LINEAGE_TASK_CONFLICTING

LOWER_LEVEL_Q2_STATUS_RETAINED:
  yes

LOWER_LEVEL_Q3_STATUS_RETAINED:
  yes
```

Maximum R5 claim:

```text
The task is conflicting because Q1 has unresolved directly
conflicting applicable lineage records; Q2 remains underdetermined
and Q3 remains established from its supplied successor relation.
```

## 7. Protocol-level reconstructed state

```text
LINEAGE_PROTOCOL_CONFORMANCE:
  LINEAGE_PROTOCOL_CONFORMANT

PROTOCOL_DEFECT_EXPOSED:
  no

PROTOCOL_REVISION_REQUIRED:
  no

SHARED_CORE_REOPEN_REQUIRED:
  no
```

## 8. Reconstructed gain/NO_GAIN metadata

This retrace reconstructs the Lineage-side task outputs only.

It does not re-execute B1 and therefore does not independently regenerate the comparative NO_GAIN judgment.

The frozen retrace may compare whether the Lineage-side outputs used by the LIN-CH-005 result are reproduced exactly.

```text
BASELINE_REEXECUTED:
  no

BASELINE_COUNTER_INCREMENT:
  no

NO_GAIN_COUNTER_INCREMENT:
  no
```

## 9. Retrace ledger freeze

```text
DERIVATION_BASIS:
  P0 + P1

P2_USED_AS_DERIVATION_SOURCE:
  no

LEDGER_STATUS:
  FROZEN_BEFORE_FORMAL_P2_COMPARISON

POST_COMPARISON_CORRECTION_ALLOWED:
  no
```
