# QM Core Reconstruction 004E — Exact GPT Theorem Interface Lock

Date: 2026-09-08  
Status: **PASS_WITH_BOUNDARY**  
Program: **Core-Theory Reconstruction Challenge**  
Target: **exactly compare the current DSD quantum-reconstruction specialization with the hypothesis set of Markus P. Mueller's finite-dimensional GPT reconstruction theorem, and prevent target-fitted assumptions from being misreported as DSD-derived results**

## 1. Purpose

QM Core 004A–004D progressively introduced or derived the following interfaces:

```text
CRD      explicit composite declaration
LDC-QM   local descriptive completeness
CRR-QM   continuous reversible reachability
RRDE-QM  restriction-recursive descriptive equivalence
OCD      operational distinguishability capacity
EDD      effect-domain declaration
NR-QM    no-restriction selector
ORD/ORE  operational randomization and randomized-preparation equivalence
MPT      mixture-preserving transformation criterion
```

The present step does **not** add another physical principle merely to move closer to quantum theory.

Instead, it locks a primary external comparator and asks:

```text
Do the current DSD specialization conditions already satisfy the exact assumptions
under which the external reconstruction theorem applies?
```

The answer is:

```text
NO — not yet exactly.
```

Several structural ingredients already match conditionally, but five theorem-interface gaps remain.

Therefore the correct status is **PASS_WITH_BOUNDARY**, not a claim that DSD has derived finite-dimensional quantum theory.

## 2. Primary external comparator

The primary comparator is:

- Markus P. Mueller, *Probabilistic Theories and Reconstructions of Quantum Theory (Les Houches 2019 lecture notes)*, arXiv:2011.01286.

The relevant GPT framework uses:

1. a real finite-dimensional vector space `A`,
2. a compact convex normalized state space `Omega_A`,
3. a linear normalization functional `u_A`,
4. the state cone generated by normalized states,
5. a dynamical state space `(A, Omega_A, T_A)` with a compact/finite group of reversible transformations,
6. the no-restriction hypothesis,
7. Tomographic Locality for composites,
8. the Subspace Axiom,
9. Continuous Reversibility.

Theorem 21 then states that, in this framework, the dynamical state space of capacity `N` is equivalent to the standard finite-dimensional complex quantum state space and unitary-conjugation reversible dynamics.

The theorem's equivalence is an invertible **linear** equivalence of state carriers that conjugates the reversible-transformation groups.

This theorem is treated only as an external conditional result.

## 3. DSD source lock

The present DSD theory does not already contain the GPT framework by definition.

### Formation layer

Formation supplies typed admission, sound restriction, sound realization, partial quantity assignment, Stage-VI channel formation, strict comparison maps, and finite composition relative to supplied post-Stage-VI term data.

It does not define a probabilistic convex state space, a tensor-product physical composite, or a connected reversible transformation group.

### Property layer

The Property Axiom System preserves model-specific property declarations, applicability, prerequisites, and partial assignments.

It therefore permits restricted physical effect families and does not imply the no-restriction hypothesis.

### Static aggregation layer

Static aggregation supplies Banach-valued downstream aggregation and explicit reconstruction-loss criteria.

Its analytic linearity is not operational mixture convexity.

### Dynamics layer

Structural Reorganization Dynamics is compatible with ordered or metric-time histories and supplied dynamic laws, but it does not generically supply the compact connected reversible group used by the GPT theorem.

Thus every interface match below must be established explicitly rather than transferred by vocabulary.

## 4. Interface map

### 4.1 Finite-dimensional real carrier — CONDITIONAL PASS

QM Core 004D showed that, for a finite separating effect/readout family,

\[
\iota(\omega)
=
(1,e_1(\omega),\ldots,e_m(\omega))
\]

embeds operational state classes into a finite-dimensional real vector space.

Let

\[
V=\operatorname{span}\iota(\Omega).
\]

This supplies a finite-dimensional real carrier without Hilbert space.

Status:

```text
PASS, conditional on finite separating readouts.
```

### 4.2 Convexity — CONDITIONAL PASS

ORD/ORE gives

\[
[R_\lambda(P,Q)]
=
\lambda[P]+(1-\lambda)[Q]
\]

on the operational quotient.

Hence the normalized state set is convex when the classical randomizer operation is declared and obeys total-probability consistency.

Status:

```text
PASS, conditional on ORD/ORE.
```

### 4.3 Normalization hyperplane / codimension one — CONDITIONAL PASS

All evaluation vectors have first coordinate one.

If `V` is defined as the span of the normalized evaluation states, then the affine hull of the normalized states lies in

\[
u(v)=1.
\]

For a nonempty spanning normalized family, the difference space has dimension

\[
\dim V-1.
\]

The finite witness in the companion Python script verifies this explicitly.

Status:

```text
PASS in the evaluation-carrier specialization.
```

### 4.4 Compactness — GAP

QM Core 004D established convexity and finite-dimensional bounded evaluation coordinates, but it did **not** establish that the physical state set is closed.

A bounded convex set need not be compact.

Counterexample:

\[
\Omega=(0,1)\subset\mathbb R
\]

is convex and bounded, but the sequence `1/n` converges to `0`, which is not in `Omega`.

Therefore:

\[
\boxed{
\text{finite-dimensional + bounded + convex}
\not\Rightarrow
\text{compact}.
}
\]

A closure/completeness condition is still required.

Proposed comparator lock:

### Operational State Closure — OSC-QM

Require the operational state image to be closed in the finite evaluation carrier.

Because the image is already bounded by probability coordinates in `[0,1]`, finite-dimensional closedness then yields compactness.

OSC-QM is a theorem-interface regularity condition, not a current generic DSD theorem.

## 5. Reversible transformations

### 5.1 Affine/linear transformation structure — CONDITIONAL PASS

MPT gives affine action on normalized operational states.

On the ordered cone generated by normalized states, an affine normalization-preserving map extends homogeneously to the cone and linearly to its span, provided the extension is well defined on the declared operational quotient.

Thus the DSD specialization can reproduce the linear transformation carrier used by the GPT framework.

Status:

```text
PASS, conditional on MPT + ordered-carrier construction.
```

### 5.2 Continuous Reversibility — CURRENT CRR-QM IS TOO WEAK

The external theorem requires:

```text
the reversible group T_N is connected
AND
it acts transitively on pure states.
```

Current CRR-QM requires continuous reversible reachability between pure states.

That does not force the **entire allowed reversible group** to be connected.

Control:

```text
O(2) is disconnected,
but its identity component SO(2) contains continuous rotation paths
that can reach any pure point of the circle from any other.
```

Thus pure-state path reachability can hold while the full allowed group remains disconnected.

Therefore:

\[
\boxed{
\text{CRR-QM}
\not\Rightarrow
\text{the theorem's Continuous Reversibility exactly}.
}
\]

Required refinement:

### Connected Reversible Group — CRG-QM

For each system type `Q_N`, the physically allowed reversible group itself must be a connected topological group and act transitively on pure states.

If the group is taken to be the identity-generated reversible subgroup, the choice must be explicit.

Status:

```text
GAP until CRG-QM is locked.
```

## 6. No-restriction — EXPLICIT SELECTOR, NOT DERIVATION

QM Core 004C already established:

\[
E_{\mathrm{phys}}
\subsetneq
E_{\max}
\]

is compatible with generic DSD.

Therefore NR-QM,

\[
E_{\mathrm{phys}}=E_{\max},
\]

must remain an explicit quantum/GPT specialization selector.

This matches the external theorem framework when adopted, but it contributes **no independent evidence** that DSD derived quantum theory.

Status:

```text
MATCH AS ASSUMPTION, NOT AS DSD CONSEQUENCE.
```

## 7. Tomographic Locality — CURRENT LDC-QM + CRD ARE NOT YET EXACTLY ENOUGH

Current LDC-QM says that all declared local-product readouts separate global states.

This captures the operational injectivity content of local tomography.

However the external GPT composite framework additionally uses:

```text
independent product preparations,
independent product effects,
AB = A tensor B as the full carrier,
u_AB = u_A tensor u_B,
T_A tensor T_B subset T_AB.
```

Current CRD requires the composite carrier and local/global maps to be explicit, but it deliberately does not force a tensor-product physical composition rule.

Therefore:

\[
\boxed{
\text{CRD + LDC-QM}
\not\Rightarrow
\text{Definition-16 Tomographic Locality interface exactly}.
}
\]

Required refinement:

### Exact Tomographic Composite Lock — ETC-QM

For the comparator specialization, require:

```text
PRODUCT_PREPARATIONS
PRODUCT_EFFECTS
TENSOR_CARRIER_EQUALITY
PRODUCT_NORMALIZATION
LOCAL_TRANSFORMATION_PRODUCTS
LOCAL_PRODUCT_READOUT_SEPARATION
```

with

\[
V_{AB}=V_A\otimes V_B,
\qquad
u_{AB}=u_A\otimes u_B,
\qquad
\mathcal T_A\otimes\mathcal T_B\subseteq\mathcal T_{AB}.
\]

LDC-QM then supplies the separation/injectivity content.

Status:

```text
GAP until ETC-QM is locked.
```

## 8. Subspace Axiom — RRDE-QM NEEDS TWO EXACT REFINEMENTS

The external Subspace Axiom quantifies over **any** measurement that perfectly distinguishes `N` states.

Current RRDE-QM was introduced using a declared maximal measurement and its excluded outcomes.

This is weaker.

A logical schema can satisfy:

```text
designated maximal measurement restriction  PASS
another maximal distinguishing measurement FAIL
```

while still satisfying the current designated version.

Therefore the quantifier must be strengthened.

Second, the theorem's lower-capacity equivalence is an invertible linear equivalence of dynamical state spaces, including conjugacy of reversible transformation groups.

Current RRDE-QM uses a typed equivalence bridge preserving the selected state/effect/readout structure and intertwining retained dynamics, but its exact GPT-linearized form must now be locked after 004D.

Required refinement:

### Universal Linear RRDE — ULRRDE-QM

For every `N`, every measurement

\[
\{e^{(1)},\ldots,e^{(N)}\}
\]

that perfectly distinguishes `N` states, and every excluded outcome `e^(i)`, require the zero-probability face to be linearly equivalent to `Q_(N-1)` and require the full subgroup of reversible transformations preserving that face to be conjugate to `T_(N-1)`.

Status:

```text
GAP until ULRRDE-QM is locked.
```

## 9. Capacity family — DEFINITION EXISTS, FAMILY EXISTENCE DOES NOT

OCD defines operational capacity without Hilbert dimension.

That matches the external operational meaning of capacity.

But the external Subspace Axiom assumes a family of system types of capacity `N` for every positive integer `N`.

Current DSD does not derive the physical existence of such a complete family.

Required comparator declaration:

### Capacity-Family Existence — CFE-QM

Declare a system type `Q_N` for each required positive integer `N`, with OCD capacity exactly `N`.

Status:

```text
GAP until CFE-QM is adopted or independently constructed.
```

## 10. Exact status table

| External theorem ingredient | Current DSD/QM specialization | Status |
|---|---|---|
| real finite-dimensional carrier | finite separating evaluation carrier | conditional pass |
| convex normalized state set | ORD/ORE | conditional pass |
| codim-one normalization slice | evaluation embedding | conditional pass |
| compact state set | bounded but closure not derived | **gap** |
| linear reversible transformations | MPT + ordered carrier | conditional pass |
| compact reversible group | needs state/group closure lock | boundary |
| no-restriction | NR-QM | explicit external selector |
| Tomographic Locality | LDC-QM + CRD | **not exact yet** |
| Subspace Axiom | RRDE-QM | **not exact yet** |
| Continuous Reversibility | CRR-QM | **not exact yet** |
| capacity definition | OCD | pass as definition |
| system family for every `N` | not derived | **gap** |

The five principal unresolved locks are:

```text
OSC-QM      operational state closure / compactness
ETC-QM      exact tomographic composite tensor lock
ULRRDE-QM   universal linear recursive restriction equivalence
CRG-QM      connected reversible group + pure-state transitivity
CFE-QM      capacity-N family existence
```

## 11. Anti-post-hoc classification

This step is also an audit against target fitting.

### Class A — pre-existing DSD structure

```text
typed formation/property separation
restriction and explicit bridges
partial assignments and status distinctions
reconstruction-loss discipline
lineage and explicit dynamics
```

These existed independently of the present quantum target.

### Class B — general operational consequences developed during the audit

```text
OCD operational capacity definition
ORD/ORE randomized-preparation convexity
MPT affine transformation consequence
finite evaluation-carrier construction
```

These are not uniquely quantum and can be tested outside quantum theory.

### Class C — quantum/GPT selectors

```text
LDC-QM
RRDE-QM
CRR-QM
NR-QM
```

These were introduced while targeting quantum reconstruction and must not be reported as generic DSD predictions.

### Class D — exact comparator locks introduced in 004E

```text
OSC-QM
ETC-QM
ULRRDE-QM
CRG-QM
CFE-QM
```

These exist only to make the external theorem interface exact.

They contribute **zero** evidence for a claim that generic DSD independently derived the theorem.

This classification is mandatory in all later synthesis.

## 12. Conditional theorem statement that will become legal only after the locks

If a DSD quantum specialization is explicitly shown to satisfy:

```text
finite-dimensional ordered carrier,
OSC-QM,
NR-QM,
ETC-QM,
ULRRDE-QM,
CRG-QM,
CFE-QM,
```

together with the already established operational mixture and capacity interfaces, then Mueller's external Theorem 21 may be invoked as a conditional result.

The legally safe statement would be:

> A DSD specialization has been constructed whose declared operational state/effect/composite/dynamical interfaces satisfy the hypotheses of a standard GPT reconstruction theorem; therefore that specialization is externally guaranteed to be linearly equivalent to finite-dimensional complex quantum state spaces with unitary-conjugation reversible dynamics.

The following stronger statements would remain invalid:

```text
DSD derives quantum mechanics from its generic axioms.
DSD proves Hilbert space without additional selectors.
The GPT theorem is a DSD theorem.
The added comparator locks are independent predictions of DSD.
```

## 13. Consequence for the research program

QM Core 004 has now reached a methodological fork.

Continuing to add exact GPT theorem assumptions would only reproduce an existing reconstruction theorem by construction.

That would be useful as an interface validation, but weak as independent DSD evidence.

Therefore after the five locks are checked, the next research move should separate into two branches:

```text
Branch I — external theorem instantiation
Complete the exact GPT interface for validation only.

Branch II — independent DSD pressure test
Return to the Class-A/B structures and ask which Class-C/D conditions
can actually be derived, weakened, replaced, or rejected using DSD's
own restriction/reconstruction/interaction structure.
```

Branch II is the scientifically stronger route for the DSD core-theory challenge.

## 14. Reproducibility

Python:

```text
audits/science/2026-09-08_qm_core_004e_exact_gpt_theorem_interface_lock.py
```

Run from repository root:

```bash
python audits/science/2026-09-08_qm_core_004e_exact_gpt_theorem_interface_lock.py --mode all
```

Expected final line:

```text
OVERALL: PASS_WITH_BOUNDARY
```

Implemented witnesses:

```text
normalized evaluation slice has codimension one
convex bounded does not imply compact
CRR-style path reachability does not imply full-group connectedness
designated RRDE quantifier does not imply universal Subspace quantifier
current CRD lacks exact tensor-locality fields
theorem-interface status matrix
```

## 15. Verdict

\[
\boxed{\textbf{PASS\_WITH\_BOUNDARY}}
\]

The exact theorem interface is **not yet closed**.

The important success is not a quantum reconstruction claim, but the opposite: the audit has located the precise places where importing the standard theorem would otherwise overstate DSD's independent content.

The five remaining exact locks are now explicit and quarantined from generic DSD claims.
