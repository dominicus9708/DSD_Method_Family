# DSD-AUDIT-20260908-MATH-027 — Collatz bounded depth-61 residue-lifting generator

Date: 2026-09-08

Verdict:

`CONFIRMED / EXACT BOUNDED GENERATOR / COMPUTATIONAL ACCELERATION`

Global status:

- Collatz conjecture: `OPEN`
- first universal Farey cell: `OPEN`

## Audited question

Can the exhaustive depth-61 right-offset stage be generated by exact surviving binary-prefix lifts rather than by independently iterating every integer `r=0..RMAX`, while preserving the complete final ordinary-integer state vector?

## DSD tuple

### D — Definition

At binary depth `k`, the generator state is

\[
(x,y,q)
\]

with

\[
x<2^k,
\qquad y=T^k(x),
\qquad q=q_k(x).
\]

For the next bit `e in {0,1}`,

\[
x'=x+e2^k.
\]

### R — Resolution

The generator preserves:

- exact ordinary prefix `x`;
- exact shortcut endpoint `y`;
- exact odd count `q`;
- exact finite bound `x<=RMAX`.

No residue-only state replaces the exact ordinary prefix.

### S — Selection

The exact lift identity is

\[
\boxed{T^k(x+e2^k)=T^k(x)+e3^q}.
\]

The parity of this lifted endpoint determines the `(k+1)`-st shortcut branch exactly. The branch is retained only if the exact depth-`k+1` coefficient threshold survives.

Branches exceeding `RMAX` are excluded because they do not belong to the finite target domain, not because of a dynamical claim.

### E — Exclusion

At every depth, a prefix that already fails the coefficient threshold cannot become a member of the audited universal-spine coefficient-surviving language at a later depth, because the language requires survival at **every prefix depth**.

Therefore early branch deletion is exact for this candidate language.

### T — Transition

From

\[
z=y+e3^q
\]

the next parity is `z mod 2`, and the exact next state is

\[
q'=q+(z\bmod2),
\]

\[
y'=\begin{cases}
z/2,&z\text{ even},\\(3z+1)/2,&z\text{ odd}.
\end{cases}
\]

The exact ordinary prefix becomes `x'`.

### C — Consistency

The generator was independently regressed against scalar enumeration at

\[
R_{\max}=10^8
\]

and

\[
R_{\max}=2\cdot10^8.
\]

The complete sorted vectors

\[
(r,T^{61}(r),q_{61})
\]

match element-by-element.

For `RMAX=2e8`:

- survivors: `358,907`;
- scalar prefix steps: `691,370,987`;
- lift branch attempts: `44,150,751`;
- peak live lift states: `2,625,819` at depth 28.

### N — Norm

Evidence status:

`ESTABLISHED_WITHIN_SCOPE`.

The arithmetic recurrence itself is exact. The stated performance counts are finite measurements on the two regression domains.

The observed approximately `15.66x` transition reduction at `2e8` is a computational-layer result, not a theorem about asymptotic complexity or Collatz density.

### O — Outcome

Established:

- exact bounded binary-prefix generator for depth-61 coefficient-surviving right offsets;
- complete full-state agreement with independent scalar enumeration on the two audited domains;
- practical stage-1 transition reduction;
- exact compatibility with the MATH-025 rolling continuation architecture.

Next open step:

- use MATH-027 as the stage-1 generator for a larger finite right-offset bootstrap;
- audit memory/state-growth behavior without introducing state aliasing.

## AP-2 audit

This construction is **not** a finite-state quotient.

Two prefixes are never merged merely because `(q,y mod 2^m)` agrees. The exact `(x,y,q)` state is retained.

Thus the MATH-023 phase-aliasing failure is not bypassed or ignored.

## Prohibited upgrades

Do not infer:

- exact bounded generator ⇒ finite automaton proof of Collatz;
- prefix pruning ⇒ arbitrary ordinary Collatz starts excluded;
- observed transition reduction ⇒ asymptotic density theorem;
- finite regression ⇒ first-cell closure.

## Reproducibility

Math-verification certificate:

`collatz/src/2026_09_08_depth61_bounded_residue_lifting_generator_certificate.cpp`

Math-verification note:

`collatz/notes/2026-09-08-depth61-bounded-residue-lifting-generator.md`

Audit ID:

`DSD-AUDIT-20260908-MATH-027`
