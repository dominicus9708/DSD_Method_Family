# DSD Audit — Collatz MATH-100 one-paid t=13 compact-carry closure

Date: 2026-09-13

Status: `PASS AS EXACT FINITE t=13 CLOSURE / NOT FIRST-CELL CLOSURE / NOT COLLATZ PROOF`

## 1. Scope

MATH-100 closes one-paid macro depth `t=13` inside the current first-cell source window. Paid-count layers `2<=r<=13` and one-paid depths `7<=t<=12` remain outside this claim.

## 2. Claim hierarchy

The audit separates:

1. phase-danger over-approximation;
2. exact same-integer dyadic compatibility;
3. Bellman terminal safety;
4. ordinary Collatz descent.

MATH-100 proves item 3 by showing that every phase-danger terminal candidate fails item 2. It does not claim a new ordinary-descent theorem for each rejected candidate.

## 3. Phase corridor direction

Stage A forgets address while retaining the lower-envelope phase/Bellman language. This enlarges the candidate language. Restoring exact address only on that danger corridor is therefore a safe one-sided pruning architecture.

`PASS`.

## 4. Compact address sufficiency

For source multiplicity `M`, let `R=ceil(log2 M)`. The state keeps

\[
X=3^{-Q}B\pmod{2^{73+R}},
\qquad
G=3^{-Q}\pmod{2^{73+R}}.
\]

The extra 73 bits cover the maximum terminal-edge resolution; the `R` low-resolution budget covers all future multi-edge consumption. Every multi transition reduces the required precision consistently with the exact normalized MATH-092 transducer.

No future compatibility query can distinguish two states identified by this retained precision.

`PASS`.

## 5. Danger threshold

Using the audited universal one-paid penalty floor `p>1/9`, any Bellman-negative `t=13` terminal must satisfy

\[
z=h-R\ge39.
\]

Thus ignoring terminal edges with `z<39` is a safe sufficient exclusion, not a heuristic cutoff.

`PASS`.

## 6. Shard coverage

Stage A produces exactly 157 actual initial danger roots. Stage B processes them independently. Cross-root deduplication is deliberately absent, so separate shards may duplicate states but cannot lose them.

The complete shard sum is:

- danger-edge attempts: `622,022,028`;
- address-compatible danger edges: `0`;
- terminal-parent occurrences: `26,061,131`;
- maximum within-root compact-state peak: `7,256,405`;
- minimum rejected residue gap: `83,441`.

The strict positive minimum gap gives an exact finite separation from address compatibility.

`PASS`.

## 7. Representation audit

The verifier retains separately:

- source multiplicity `M`;
- source/modulus depth `H`;
- phase-cell ID;
- normalized target address `X`;
- normalized odd multiplier inverse `G`.

It does not replace integer identity by density or probability. The normalized address is an exact quotient representation of the original affine family.

`PASS`.

## 8. Prohibited upgrades

Do not infer:

- `t=13 CLOSED => 7<=t<=12 CLOSED`;
- one-paid closure at one depth `=>` first universal cell closure;
- Bellman-danger address exclusion `=>` every represented integer has been independently descended to `2^71`;
- one-paid result `=>` paid-count `r=13` closure;
- MATH-100 `=>` Collatz conjecture.

## 9. Verdict

MATH-100 passes as an **exact finite closure of the one-paid macro-depth-13 Bellman danger language**.

The remaining detailed one-paid frontier is

\[
\boxed{7\le t\le12}.
\]
